import ControlsNavigator from "./ControlsNavigator";
import Vec3 from "~/lib/math/Vec3";
import CursorStyleSystem from "../systems/CursorStyleSystem";
import { ControlsState } from "../systems/ControlsSystem";
import PerspectiveCamera from "~/lib/core/PerspectiveCamera";
import TerrainHeightProvider from "../terrain/TerrainHeightProvider";
export default class GroundControlsNavigator extends ControlsNavigator {
    private readonly camera;
    private readonly cursorStyleSystem;
    private readonly terrainHeightProvider;
    target: Vec3;
    private direction;
    distance: number;
    private distanceTarget;
    pitch: number;
    yaw: number;
    private isLMBDown;
    private isRMBDown;
    private LMBDownPosition;
    private lastLMBMoveEvent;
    private forwardKeyPressed;
    private leftKeyPressed;
    private rightKeyPressed;
    private backwardKeyPressed;
    private fastMovementKeyPressed;
    private pitchMinusKeyPressed;
    private pitchPlusKeyPressed;
    private yawMinusKeyPressed;
    private yawPlusKeyPressed;
    private pointerPosition;
    private isInTransition;
    private transitionType;
    private transitionStart;
    slippyMapOverlayFactor: number;
    switchToSlippy: boolean;
    private transitionYawFrom;
    private transitionPitchFrom;
    constructor(element: HTMLElement, camera: PerspectiveCamera, cursorStyleSystem: CursorStyleSystem, terrainHeightProvider: TerrainHeightProvider);
    private addEventListeners;
    lookAtNorth(): void;
    private mouseDownEvent;
    private mouseLeaveEvent;
    private mouseUpEvent;
    private mouseMoveEvent;
    private wheelEvent;
    private keyDownEvent;
    private keyUpEvent;
    private moveTarget;
    private projectOnGround;
    private projectOnGroundPredict;
    private updateDistance;
    private clampPitchAndYaw;
    private updateTargetHeightFromHeightmap;
    private updateCameraProjectionMatrix;
    syncWithCamera(prevNavigator: ControlsNavigator): void;
    syncWithState(state: ControlsState): void;
    getCurrentState(): ControlsState;
    disable(): void;
    private startTransition;
    private processMovementByKeys;
    private doTransition;
    update(deltaTime: number): void;
}
//# sourceMappingURL=GroundControlsNavigator.d.ts.map