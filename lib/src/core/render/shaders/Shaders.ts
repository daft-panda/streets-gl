// Import from our virtual module created by the Rollup plugin
// The shader includes are already resolved during the build process
import Shaders from 'shader-files-context';

// Export the shaders collection
export default Shaders;